html
  head
    style
      include style.css
  body
    each feed in feeds
      h1= feed.name
      each item in feed.items
        li
          if item.url
            a(href=item.url)= item.title
          else 
            a(href=item.link)= item.title
